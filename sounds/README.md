# Sound assets

These short WAV cues ship with `write-or-die` under the package's
GPL-3.0-or-later license.

Audio is optional. Set `write-or-die-sound-backend` to `none`, or point
`write-or-die-sound-directory` at your own WAV files.
